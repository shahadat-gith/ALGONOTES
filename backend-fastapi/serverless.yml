service: algonotes-backend-fastapi

frameworkVersion: "4"

# ==========================================
# PROVIDER CONFIGURATION
# ==========================================
provider:
  name: aws
  runtime: python3.12
  region: ap-south-1
  stage: prod
  memorySize: 1024

  environment:
    ENVIRONMENT: production
    PORT: ${env:PORT}
    DATABASE_URL: ${env:DATABASE_URL}
    JWT_SECRET: ${env:JWT_SECRET}
    JWT_EXPIRES_IN: ${env:JWT_EXPIRES_IN}

    CLOUDINARY_CLOUD_NAME: ${env:CLOUDINARY_CLOUD_NAME}
    CLOUDINARY_API_KEY: ${env:CLOUDINARY_API_KEY}
    CLOUDINARY_API_SECRET: ${env:CLOUDINARY_API_SECRET}

    MAIL_SERVER: ${env:MAIL_SERVER}
    MAIL_PORT: ${env:MAIL_PORT}
    MAIL_USERNAME: ${env:MAIL_USERNAME}
    MAIL_PASSWORD: ${env:MAIL_PASSWORD}
    MAIL_FROM: ${env:MAIL_FROM}
    MAIL_FROM_NAME: ${env:MAIL_FROM_NAME}

    GEMINI_API_KEY: ${env:GEMINI_API_KEY}

    # Points explicitly to the isolated FastAPI SQS queue instance
    AI_GENERATION_QUEUE_URL:
      Ref: FastapiAiGenerationQueue

  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - sqs:SendMessage
            - sqs:ReceiveMessage
            - sqs:DeleteMessage
            - sqs:GetQueueAttributes
          Resource:
            Fn::GetAtt:
              - FastapiAiGenerationQueue
              - Arn

# ==========================================
# LAMBDA FUNCTIONS
# ==========================================
functions:
  # Main FastAPI Application Handler
  api:
    handler: main.handler
    events:
      - httpApi:
          path: /
          method: ANY
      - httpApi:
          path: /{proxy+}
          method: ANY

  # Background Asynchronous SQS Worker (FastAPI specific)
  aiWorker:
    handler: app.sqs.handler.handler
    timeout: 300
    memorySize: 1024
    events:
      - sqs:
          arn:
            Fn::GetAtt:
              - FastapiAiGenerationQueue
              - Arn
          batchSize: 1
          functionResponseType: ReportBatchItemFailures

# ==========================================
# CLOUDFORMATION RESOURCES (SQS)
# ==========================================
resources:
  Resources:
    FastapiAiGenerationQueue:
      Type: AWS::SQS::Queue
      Properties:
        # Unique physical queue channel name to separate it from Express
        QueueName: algonotes-fastapi-ai-generation-queue
        VisibilityTimeout: 360
        MessageRetentionPeriod: 86400

# ==========================================
# CUSTOM PLUGINS CONFIGURATION
# ==========================================
custom:
  pythonRequirements:
    useDownloadCache: false
    useStaticCache: false
    pipCmdExtraArgs:
      - "--platform"
      - "manylinux2014_x86_64"
      - "--only-binary=:all:"
    slim: false

# ==========================================
# PACKAGING POLICIES
# ==========================================
package:
  patterns:
    - "app/**"              
    - "main.py"
    - "!env/**"
    - "!venv/**"
    - "!node_modules/**"
    - "!.env"
    - "!__pycache__/**"
    - "!Dockerfile"
    - "!.dockerignore"